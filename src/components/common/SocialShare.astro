---
// Intentionally inert: social sharing was removed from the Sinar Farma build.
// Retained as a safe no-op component for any legacy imports.
export interface Props {
  text?: string;
  url?: string | URL;
  class?: string;
}
---
